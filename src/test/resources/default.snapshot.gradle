buildscript {
	repositories {
		maven { url 'https://repo.spring.io/libs-snapshot' }
	}
}
plugins {
	id 'ru.itbasis.gradle.plugins.spring-boot-module-gradle-plugin'
}
repositories {
	maven { url 'https://repo.spring.io/libs-snapshot' }
}
