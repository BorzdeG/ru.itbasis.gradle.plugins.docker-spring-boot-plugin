buildscript {
	repositories {
		maven { url 'https://repo.spring.io/libs-snapshot' }
	}
}
plugins {
	id 'ru.itbasis.gradle.plugins.docker-spring-boot-plugin'
}
repositories {
	maven { url 'https://repo.spring.io/libs-snapshot' }
}
